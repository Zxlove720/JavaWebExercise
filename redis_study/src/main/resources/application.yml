spring:
  application:
    name: redis_study
  data:
    redis:
      host: 192.168.181.130
      port: 6379
      password: 123456
      lettuce:
        pool:
          max-active: 8 # 最大连接数
          max-idle: 8 # 最大空闲连接
          min-idle: 0 # 最小空闲连接
          max-wait: 100ms # 连接等待时间
